/// <reference types="cypress" />

describe('ticket booking', () => {
    // beforeEach(() => {
    //   cy.visit('http://localhost:3000/book')
    // })
  
    // it('filters films by film or date', () => {
    //   cy.get('.filmBlock').should('have.length', 3)

    // })
  })
  